services:
  subgen:
    build: .
    env_file: .env
    environment:
      SUBGEN_TEMP_DIR: /tmp/subgen
    tmpfs:
      - /tmp/subgen:size=8g
    volumes:
      # read-write so SRTs can be written back next to the source videos
      - ${HOME}:${HOME}
      - ./out:/out